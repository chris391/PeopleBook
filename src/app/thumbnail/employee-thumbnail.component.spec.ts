/*
import {async, TestBed} from "@angular/core/testing";
import {EmployeeThumbnailComponent} from "./employee-thumbnail.component";

it('should render h2 in a h2 tag', async(() => {
 let fixture = TestBed.createComponent(EmployeeThumbnailComponent);
 fixture.detectChanges();
 let compiled = fixture.debugElement.nativeElement;
 expect(compiled.querySelector('h2').textContent).toContain('employee.name');
 }));
*/
